#include <iostream>
#include <string.h>
#include <vector>
using namespace std;


class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        int k = 0;
        
        for(int i; i < sizeof(nums) / nums[i]; i++) {
            cout << nums[i];
        }

        return k;
    }
};

int main() {
    Solution solution;

    
    return 0;
}